package com.safeharborsecurity.app.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Palette — light theme
val NavyBlue = Color(0xFF1A3A5C)         // Brand color — headers, accents on light bg
val NavyBlueDark = Color(0xFF0D1B2A)     // Very dark — used sparingly
val NavyBlueLight = Color(0xFF2A5080)    // Medium blue accent
val WarmGold = Color(0xFFE8A833)         // Gold accent — buttons, highlights
val WarmGoldDark = Color(0xFFCC9020)
val WarmGoldLight = Color(0xFFFFF3E0)    // Light gold background

// Background & Surface — light theme
val WarmWhite = Color(0xFFF5F7FA)        // App background — very light blue-grey
val WarmWhiteLight = Color(0xFFFFFFFF)   // Pure white
val LightSurface = Color(0xFFFFFFFF)     // Card backgrounds — white

// Semantic Colors — adjusted for light backgrounds
val ScamRed = Color(0xFFD32F2F)
val ScamRedLight = Color(0xFFFFEBEE)
val WarningAmber = Color(0xFFF57C00)
val WarningAmberLight = Color(0xFFFFF3E0)
val SafeGreen = Color(0xFF2E7D32)
val SafeGreenLight = Color(0xFFE8F5E9)

// Text — for light backgrounds
val TextPrimary = Color(0xFF1A1A1A)      // Near-black — primary text
val TextSecondary = Color(0xFF78909C)    // Blue-grey — secondary text
val TextOnDark = Color(0xFFF0F0F0)       // Light text on dark surfaces (e.g. NavyBlue top bars)
val TextOnGold = Color(0xFF1A1A1A)       // Dark text on gold buttons

// Bubble Colors — chat
val UserBubble = Color(0xFF1A3A5C)       // Dark navy bubble
val SafeHarborBubble = Color(0xFFE8F5E9) // Light green bubble
val SafeHarborBubbleBorder = Color(0xFFE8A833)

// Status Ring
val PulseGreen = Color(0xFF4CAF50)
val PulseAmber = Color(0xFFFFC107)

// Teal accent for specific buttons
val TealAccent = Color(0xFF00897B)
